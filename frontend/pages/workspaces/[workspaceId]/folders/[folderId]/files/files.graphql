query FilesByFolder($folder: ID!) {
  filesByFolder(folder: $folder) {
    id
    title
    description
    fileType
    fileName
    modifiedAt
  }
}

query GetFileById($id: ID!) {
  file(id: $id) {
    id
    title
    description
    fileType
    fileName
    modifiedAt
  }
}
